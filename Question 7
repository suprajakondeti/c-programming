#include <stdio.h>

 void main() {

    int num, count = 0;

    printf("Enter an integer: ");

    scanf("%d", &num);

    while(num != 0) {

        count++;

        num /= 10;

    }

    printf("Number of digits: %d", count);

    getch();

}
